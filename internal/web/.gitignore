node_modules/

# Built bundles are regenerated by `npm run build` (and in CI/Docker before the
# Go build). The committed dist/index.html + dist/favicon.svg let the Go binary
# build from a clean checkout; the bundled assets are not committed.
dist/assets/

# Playwright + test artifacts
test-results/
playwright-report/
screenshots/
