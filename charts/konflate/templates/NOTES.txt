konflate is reviewing {{ .Values.config.repo }}.

Open the UI:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "konflate.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}
  # then browse http://localhost:{{ .Values.service.port }}

{{ if .Values.ingress.enabled -}}
Or via ingress:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{ end -}}

{{- if not (include "konflate.secretName" .) }}
No forge token or inbound secrets are configured: konflate makes unauthenticated
(anonymous) forge requests — always read-only, but subject to the forge's lower
anonymous rate limit — and the webhook (POST /hooks) and push endpoints are
disabled. Open PRs still re-render automatically every {{ .Values.config.refreshInterval | default "30m" }}.

Set `secret.token` to raise the rate limit and review private repos; set
`secret.webhookSecret` / `secret.pushToken` to enable inbound re-render triggers.
{{- end }}
